// Legacy test left here intentionally but disabled.
// It tried to inject sessions into the running app and was flaky.
// Replaced by `access-control.test.js`.
test.skip('legacy rbac test (disabled)', () => {});
