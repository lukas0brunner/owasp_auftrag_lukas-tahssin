name: m183_lb2_db
services:
    db:
      build: ./db
      container_name: m183-lb2-db
      expose:
        - "3306"
      env_file:
        - .env
      environment:
        - MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD}

